"""Construction and validation of non-semantic arithmetic graphs.

The pipeline is explicit:

``parser -> builder -> execution/postfix``

The :mod:`audit` module validates that pipeline outside training.
"""
